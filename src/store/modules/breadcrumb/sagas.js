// import { all, takeLatest, call, put } from "redux-saga/effects"

// // import { updateCrumb } from "./actions"

// export function upCrumb({ payload }) {
//   const { label, to, order, key } = payload.crumb

//   console.tron.log(label, to, order, key)
// }

// export default all([takeLatest("UPDATE_CRUMB", upCrumb)])
