import styled from 'styled-components';
import {Row as RowGrid, Col as ColGrid, Grid as GridGrid} from 'react-flexbox-grid' 

export const Row = styled(RowGrid)`
`;

export const Col = styled(ColGrid)`
`;

export const Grid = styled(GridGrid)`
  
`;